# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

`recap-flow-transaction` is a new, uninitialized project. No source files, build configuration, or dependencies have been added yet.

Once the project is initialized, update this file with:

- **Build/dev/test commands** — e.g. `npm run dev`, `npm test`, `npm run lint`
- **Architecture overview** — main modules, data flow, key abstractions
- **Environment setup** — required env vars, external services, local setup steps
